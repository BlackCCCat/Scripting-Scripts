## 本次更新

- 新增更新内容提示：当这份更新说明发生变化后，打开 PhotoSlide 会自动弹出一次。
- 支持全量读取照片，并通过附近图片懒加载降低浏览大量照片时的压力。
- 新增相簿、截图和日期区间筛选，可以只整理指定范围内的照片。
- 支持将照片加入相簿、收藏、跳过、加入待删除，并把相簿移动也纳入撤销记录。
- 调整滑动手势：上滑加入待删除，下滑跳过；在已跳过列表中下滑可取消跳过。
- 优化图片显示为自适应完整显示，截图和长图不再被裁切放大。

## 说明

更新提示仅在 `release-notes.md` 内容变化后再次出现，关闭后不会在同一份内容下重复打扰。
