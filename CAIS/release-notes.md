## 本次更新
- 设置增加玻璃效果开关，低版本系统默认关闭
- 优化首页UI的启动响应

## 近期更新
- 设置等内容现在全部写入CAIS私有Storage，不再写入共享Storage
- 调整首页UI
- 修复低版本系统上页面显示问题


## 使用提示

- 网络请求脚本可直接使用当前条目内容参数 `text`，也可以在字符串中使用 `{{text}}`。
- 更新说明只根据 Markdown 内容变化判断是否再次显示；脚本版本号变化但说明内容不变时不会重复弹出。
