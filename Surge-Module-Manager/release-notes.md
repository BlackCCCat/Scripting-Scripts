## 本次更新

- 新增模块图标显示：优先读取模块内 `#icon` / `#!icon` 链接，没有图标时按远程/本地模块显示对应 SF Symbol。
- 新增 iCloud 元数据备份/同步：可导出、导入模块链接、分类、相对目录和图标等信息。
- 优化快捷指令更新：快捷指令调用时使用更轻量的直接下载写入流程，并返回调试结果。
- 优化模块列表筛选：子文件夹和分类筛选改为单行菜单，长文本不再换行遮挡。

## 使用说明

- 更新内容弹窗只会在本文件内容变化后再次显示。
- 关闭弹窗后会自动标记为已读。
