## 本次新增

- 新增更新内容提示：当本文件内容变化后，启动脚本会自动显示一次更新说明。
- 新增代理 App 切换，可在 Surge、Quantumult X、Loon、Stash、Shadowrocket、Egern 之间选择。
- Surge 模式下支持通过 HTTP API 检查并控制 WLOC 模块开关。
- 底部操作区增加常驻“移除定位”按钮，无已保存定位时自动禁用。

## 修复

- 当前生效坐标不再使用脚本本地缓存兜底，只以 WLOC 模块查询结果为准。
- 模块未开启或查询失败时，底部卡片会显示“设备无已保存坐标”，避免误判为已有定位。
